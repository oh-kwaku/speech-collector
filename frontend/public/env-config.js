// Placeholder for local dev / `vite build` without Docker. In the production
// image this file is regenerated from the container's real environment
// variables at startup (see docker-entrypoint.d/env-config.sh), so Dokploy's
// Environment tab takes effect without a rebuild.
window.__RUNTIME_CONFIG__ = {}
